# Environment supplement

You are in a Claude Code session. After running `npx --yes skills add blaxel-ai/agent-skills -g --skill blaxel-cli blaxel-sdk -y`, restart this session or open a new one so the Blaxel skill loads. Project-scope settings: `.claude/settings.json`. User-scope: `~/.claude/settings.json`.
