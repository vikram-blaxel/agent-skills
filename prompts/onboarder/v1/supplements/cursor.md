# Environment supplement

You are in Cursor. After running `npx --yes skills add blaxel-ai/agent-skills -g --skill blaxel-cli blaxel-sdk -y`, reload the Cursor window so the Blaxel skill loads. MCP config: `~/.cursor/mcp.json`. Project rules: `.cursorrules`.
