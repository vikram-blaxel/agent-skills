# Environment supplement

You are in a Codex CLI session. After running `npx --yes skills add blaxel-ai/agent-skills -g --skill blaxel-cli blaxel-sdk -y`, restart Codex or run `/reload` so the Blaxel skill loads in this session. Codex config lives at `~/.codex/config.toml`.
